<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.zhanghao.gankio">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>
    <uses-permission android:name="android.permission.READ_PHONE_STATE"/>

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>

    
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:name=".BaseApplication"
        android:theme="@style/AppTheme">

        <provider
            android:authorities="com.zhanghao.gankio.fileprovider"
            android:name="android.support.v4.content.FileProvider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/filepaths"/>
        </provider>


        <activity
            android:name=".ui.activity.MainActivity"
            android:label="@string/app_name">
        </activity>
        <activity android:name=".ui.activity.SettingActivity"/>
        <activity android:name=".ui.activity.DetailActivity" />
        <activity android:name=".ui.activity.GankTypeListActivity"/>
        <activity android:name=".ui.activity.PhotoActivity"/>
        <activity android:name=".ui.activity.FavActivity"/>
        <activity android:name=".ui.activity.SearchActivity"
            android:theme="@style/SearchActivityTheme"
            />
        <activity android:name=".ui.activity.SplashActivity">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
        </activity>

        <service android:name=".service.SplashImageService"
            android:exported="false"
            />

        <service android:name=".service.FirRemoteService"
            android:exported="false"/>


        <meta-data android:value="" android:name="BUG_HD_SDK_GENERAL_KEY" />

    </application>

</manifest>